;  the methods below are called by tvSpellHandler and tvSpellObj to create/delete and manipulate
;  dictionaries that you store in your database
;  the spell checker dictionary window has an option to store a dictionary that
;  a user creates in your database

;  you must provide a Omnis database-file or sql-table to store the dictionaries
;  the code as it stands will not work, local stub-variables have been used to show you
;  where you need to put your database fields

;  storing dictionaries in your database allows users to share them